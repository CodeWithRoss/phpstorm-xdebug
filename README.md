

To Check xdebug is loaded and running, you can run the following command.

`docker compose exec app php -v`

You should see `with Xdebug v3.x.x`


/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini